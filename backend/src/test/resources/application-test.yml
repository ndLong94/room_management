spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
  batch:
    job:
      enabled: false

logging:
  level:
    root: WARN
    com.management: INFO

app:
  jwt:
    secret: test-jwt-secret-at-least-32-characters-long-for-it
  upload:
    dir: ${java.io.tmpdir}/management-test-uploads
