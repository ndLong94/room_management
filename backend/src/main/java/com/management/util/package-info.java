/**
 * Small, reusable helpers (formatting, trimming). Add types here only when at least two
 * call sites need the same behavior — see project rules {@code java-reuse-utils.mdc}.
 */
package com.management.util;
